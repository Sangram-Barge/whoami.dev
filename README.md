# whoami.dev — Terminal-style React portfolio

Quick start:
1. Clone this repo.
2. `npm install`
3. `npm run dev` to start the dev server.
4. `npm run build` then `npm run deploy` to deploy to GitHub Pages (if you set up gh-pages) or use the included GitHub Actions workflow.

Replace the placeholder text in `src/components/Terminal.jsx` with your details.
